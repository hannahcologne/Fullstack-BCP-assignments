/*Prompt

Index Of

Write a function indexOf that accepts two arguments: an array and an element to search for. The function should return the index 
of the element if found in the array and -1 if not.

indexOf([1,2,3], 2)    // => 1
indexOf(["hello","world"], "goodbye") 

*/

function indexOf(array, searchElement) {

	for(var index = 0; index < array.length; index++) {
		if(array.includes(searchElement)) {
		  return array.indexOf(searchElement);
 		} else {
 		  return -1;
 		}
	} 
}

//indexOf([1,2,3], 2)    // => 1
//indexOf(["hello","world"], "goodbye") // >> -1 
